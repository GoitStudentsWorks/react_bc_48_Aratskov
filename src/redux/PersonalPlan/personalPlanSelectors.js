export const selectYear = state => state.plan.year;
export const selectMonth = state => state.plan.month;
export const selectPersonalPlan = state => state.plan;
// export const selectId = state => Boolean(state.plan._id);